apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-app
  namespace: edu-roman-kolyada
spec:
  replicas: 1
  selector:
    matchLabels:
      app: django
  template:
    metadata:
      labels:
        app: django
    spec:
      containers:
      - name: django
        image: romigo24/django-site:e28d3c0
        ports:
        - containerPort: 8000
        env:
        - name: DJANGO_SETTINGS_MODULE
          value: "webapp.settings"
        - name: PYTHONUNBUFFERED
          value: "1"
        envFrom:
        - secretRef:
            name: postgres
        - secretRef:
            name: django-secrets
        volumeMounts:
        - name: ssl-cert-volume
          mountPath: /root/.postgresql
          readOnly: true
      volumes:
      - name: ssl-cert-volume
        secret:
          secretName: postgres-ssl-cert
          defaultMode: 0600